Make sure you set the ${app.version} ${app.name} in your application.properties or application.yml.

application yml:
app:
  name: demoapp
  version: 1.0.0

  application.properties
  app.name=demoapp
  app.version=1.0.0
